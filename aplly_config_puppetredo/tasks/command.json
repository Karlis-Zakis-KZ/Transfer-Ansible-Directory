{
    "description": "Run a command on Cisco IOS",
    "parameters": {
      "commands": {
        "type": "Array[String]",
        "description": "The commands to run"
      }
    },
    "implementations": [
      {"name": "command.sh", "requirements": ["shell"]}
    ]
  }
  