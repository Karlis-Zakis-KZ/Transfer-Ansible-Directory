{
    "description": "Apply a configuration on Cisco IOS",
    "parameters": {
      "lines": {
        "type": "Array[String]",
        "description": "The configuration lines to apply"
      },
      "parents": {
        "type": "Array[String]",
        "description": "The parent configuration context"
      }
    },
    "implementations": [
      {"name": "config.sh", "requirements": ["shell"]}
    ]
  }
  