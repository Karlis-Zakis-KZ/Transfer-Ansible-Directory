{
    "description": "Configure network interface and capture packets using Netmiko",
    "parameters": {
      "interface": {
        "description": "The network interface to monitor for packet capture",
        "type": "String"
      },
      "runs": {
        "description": "The number of times to run the configuration",
        "type": "Integer"
      }
    },
    "implementations": [
      {"name": "network_config_task.sh", "requirements": ["shell"]}
    ]
  }
  